import { HomeTemplate, ImagesLoadedTemplate } from '@/components/templates';

const Home = () => {
  return (
    <ImagesLoadedTemplate>
      <HomeTemplate />
    </ImagesLoadedTemplate>
  );
};

export default Home;
